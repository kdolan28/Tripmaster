<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Master - Search Results</title>
    <link rel="stylesheet" href="assets/style.css">
    <script src=""></script>
    <script src="assets/results.js"></script>

</head>
<body>
    <!-- Header -->
    <header>
        <div class="logo">Travel Master</div>
        <nav>
            <ul>
                <li><a href="index.html">Home</a></li>
                <li><a href="#">Hotels</a></li>
                <li><a href="#">Flights</a></li>
                <li><a href="#">Sign In/Sign Up</a></li>
            </ul>
        </nav>
    </header>

    <!-- Search Results Page -->
    <main>
        <section class="search-results">
            <h2>Search Results</h2>
            <div class="result-category">
                <h3>Destination</h3>
                <div id="destinationResults"></div>
            </div>
            <div class="result-category">
                <h3>Hotels</h3>
                <div id="hotelResults" class="results-list"></div>
            </div>
            <div class="result-category">
                <h3>Flights</h3>
                <div id="flightResults" class="results-list"></div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer>
        <div>Contact Information</div>
        <div>About Us</div>
        <div>Social Media Links</div>
    </footer>

    <!-- JavaScript -->
    <script src="assets/script.js"></script>
    <script>
        // Function to get search parameters and display results
        function getSearchParameters() {
            const params = new URLSearchParams(window.location.search);
            const destination = params.get('destination');
            const checkInDate = params.get('checkIn');
            const checkOutDate = params.get('checkOut');
            const guests = params.get('guests');
            // Implement your search logic here, and update the DOM with results
            document.getElementById('destinationResults').innerHTML = `Results for ${destination}`;
            // Add logic to fetch and display hotel and flight results
        }

        document.addEventListener('DOMContentLoaded', getSearchParameters);
    </script>
</body>
</html>
